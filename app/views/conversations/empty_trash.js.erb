 
 
 $.smallBox({
	title : "<%= t(:mailbox, scope: 'modules') %>",
	content : "<%= t(:trash_emptied_message) %>",
	color : "#4c4f53",
	iconSmall : "fa fa-envelope-o bounce animated",
	timeout : 2000,
	sound: false
});
 

loadURL('<%= mailbox_trash_path %>',  $(".ajax-notifications"));

